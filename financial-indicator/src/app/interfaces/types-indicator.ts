import { Indicator } from './indicator';

export interface Dollar {
    Dolares: Array<Indicator>;
}

export interface Euro {
    Euros: Array<Indicator>;
}

export interface CPI {
    IPCs: Array<Indicator>;
}

export interface UF {
    UFs: Array<Indicator>;
}

export interface UTM {
    UTMs: Array<Indicator>;
}
